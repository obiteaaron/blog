VIM常用配置

编辑环境配置
```
#vim ~/.vimrc
```

```
syntax on #语法高亮开启
set autoindent #自动缩进开启
set cindent #启用cindent缩进结构，会覆盖autoindent设置
set number #启用显示行号
set tabstop=4 #设定tab宽度为4个字符
set shiftwidth=4 #设定自动缩进为4个字符
set expandtab #用space替代tab的输入
```

